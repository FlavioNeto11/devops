# Aponte para o IP da máquina onde o backend roda na sua LAN.
# Edite também `extra.apiUrl` em app.json (é o que o app lê em runtime).
# Ex.: http://192.168.0.10:3000  (NÃO use localhost no dispositivo físico)
API_URL=http://192.168.0.10:3000
