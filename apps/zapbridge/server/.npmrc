# O peer `multer >=1.4` do @flavioneto11/ai-ingest-middleware não casa com o prerelease
# `multer@1.4.5-lts.*` (semver trata `-lts` como pré-lançamento). Mantemos o multer 1.x
# (upload já funcionando) e toleramos o peer com legacy-peer-deps — vale p/ install e ci.
legacy-peer-deps=true
