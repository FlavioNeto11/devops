# TeamFlow — Operação de Equipes — k8s gerado pela Forge (gymops-style). Imagens :local.
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: teamflow-postgres, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow } }
spec: { accessModes: [ReadWriteOnce], resources: { requests: { storage: 1Gi } } }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: teamflow-postgres, namespace: apps, labels: { app.kubernetes.io/name: teamflow-postgres, app.kubernetes.io/part-of: teamflow } }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector: { matchLabels: { app.kubernetes.io/name: teamflow-postgres } }
  template:
    metadata: { labels: { app.kubernetes.io/name: teamflow-postgres, app.kubernetes.io/part-of: teamflow } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom: [ { secretRef: { name: teamflow-db } } ]
          ports: [ { containerPort: 5432 } ]
          volumeMounts: [ { name: data, mountPath: /var/lib/postgresql/data, subPath: pgdata } ]
      volumes: [ { name: data, persistentVolumeClaim: { claimName: teamflow-postgres } } ]
---
apiVersion: v1
kind: Service
metadata: { name: teamflow-postgres, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow } }
spec: { selector: { app.kubernetes.io/name: teamflow-postgres }, ports: [ { port: 5432, targetPort: 5432 } ] }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: teamflow-redis, namespace: apps, labels: { app.kubernetes.io/name: teamflow-redis, app.kubernetes.io/part-of: teamflow } }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: teamflow-redis } }
  template:
    metadata: { labels: { app.kubernetes.io/name: teamflow-redis, app.kubernetes.io/part-of: teamflow } }
    spec: { containers: [ { name: redis, image: redis:7-alpine, ports: [ { containerPort: 6379 } ] } ] }
---
apiVersion: v1
kind: Service
metadata: { name: teamflow-redis, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow } }
spec: { selector: { app.kubernetes.io/name: teamflow-redis }, ports: [ { port: 6379, targetPort: 6379 } ] }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: teamflow-api
  namespace: apps
  labels: { app.kubernetes.io/name: teamflow-api, app.kubernetes.io/component: api, app.kubernetes.io/part-of: teamflow, devops.flavioneto/app-type: product_software }
  annotations: { devops.flavioneto/app: teamflow, devops.flavioneto/metrics-port: "9464" }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: teamflow-api } }
  template:
    metadata: { labels: { app.kubernetes.io/name: teamflow-api, app.kubernetes.io/component: api, app.kubernetes.io/part-of: teamflow } }
    spec:
      containers:
        - name: api
          image: teamflow-api:local
          imagePullPolicy: IfNotPresent
          command: ["npm", "start"]
          envFrom: [ { secretRef: { name: teamflow-db } } ]
          env:
            - { name: REDIS_URL, value: "redis://teamflow-redis:6379" }
            - { name: METRICS_PORT, value: "9464" }
            - { name: AUTO_MIGRATE, value: "true" }
            - { name: AUTO_SEED, value: "true" }
          ports:
            - { name: http, containerPort: 8080 }
            - { name: metrics, containerPort: 9464 }
          readinessProbe: { httpGet: { path: /health, port: 8080 }, initialDelaySeconds: 8, periodSeconds: 10 }
---
apiVersion: v1
kind: Service
metadata: { name: teamflow-api, namespace: apps, labels: { app.kubernetes.io/name: teamflow-api, app.kubernetes.io/part-of: teamflow } }
spec: { selector: { app.kubernetes.io/name: teamflow-api }, ports: [ { name: http, port: 8080, targetPort: 8080 }, { name: metrics, port: 9464, targetPort: 9464 } ] }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: teamflow-worker
  namespace: apps
  labels: { app.kubernetes.io/name: teamflow-worker, app.kubernetes.io/component: worker, app.kubernetes.io/part-of: teamflow }
  annotations: { devops.flavioneto/app: teamflow, devops.flavioneto/metrics-port: "9464" }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: teamflow-worker } }
  template:
    metadata: { labels: { app.kubernetes.io/name: teamflow-worker, app.kubernetes.io/component: worker, app.kubernetes.io/part-of: teamflow } }
    spec:
      containers:
        - name: worker
          image: teamflow-api:local
          imagePullPolicy: IfNotPresent
          command: ["npm", "run", "worker"]
          envFrom: [ { secretRef: { name: teamflow-db } } ]
          env:
            - { name: REDIS_URL, value: "redis://teamflow-redis:6379" }
            - { name: METRICS_PORT, value: "9464" }
            - { name: AUTO_MIGRATE, value: "false" }
          ports: [ { name: metrics, containerPort: 9464 } ]
---
apiVersion: v1
kind: Service
metadata: { name: teamflow-worker, namespace: apps, labels: { app.kubernetes.io/name: teamflow-worker, app.kubernetes.io/part-of: teamflow } }
spec: { selector: { app.kubernetes.io/name: teamflow-worker }, ports: [ { name: metrics, port: 9464, targetPort: 9464 } ] }
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata: { name: teamflow-api-strip, namespace: apps }
spec: { stripPrefix: { prefixes: ["/teamflow/api"] } }
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata: { name: teamflow-root-strip, namespace: apps }
spec: { stripPrefix: { prefixes: ["/teamflow"] } }
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: teamflow, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow } }
spec:
  entryPoints: [web]
  routes:
    - match: PathPrefix(`/teamflow/api`)
      kind: Rule
      priority: 40
      services: [ { name: teamflow-api, port: 8080 } ]
      middlewares: [ { name: console-auth-401, namespace: devops-system }, { name: teamflow-api-strip } ]
    # raiz pública (landing) — SEM o gate SSO, p/ a página de status ser navegável
    - match: PathPrefix(`/teamflow`)
      kind: Rule
      priority: 10
      services: [ { name: teamflow-api, port: 8080 } ]
      middlewares: [ { name: teamflow-root-strip } ]
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: teamflow, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow, release: kube-prometheus-stack } }
spec:
  selector: { matchLabels: { app.kubernetes.io/part-of: teamflow } }
  namespaceSelector: { matchNames: [apps] }
  endpoints: [ { port: metrics, path: /metrics, interval: 15s } ]
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata: { name: teamflow-slo, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow, release: kube-prometheus-stack } }
spec:
  groups:
    - name: teamflow.slo
      rules:
        - alert: teamflowApiDown
          expr: absent(up{job=~".*teamflow-api.*"} == 1)
          for: 3m
          labels: { severity: critical, app: teamflow }
          annotations: { summary: "teamflow: API sem target UP" }
