# TeamFlow — Operação de Equipes — frontend (Vue SPA) gerado pela Forge. Aditivo ao k8s do backend.
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: teamflow-frontend, namespace: apps, labels: { app.kubernetes.io/name: teamflow-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: teamflow } }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: teamflow-frontend } }
  template:
    metadata: { labels: { app.kubernetes.io/name: teamflow-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: teamflow } }
    spec: { containers: [ { name: frontend, image: teamflow-frontend:local, imagePullPolicy: IfNotPresent, ports: [ { containerPort: 80 } ], readinessProbe: { httpGet: { path: /healthz, port: 80 }, initialDelaySeconds: 3, periodSeconds: 10 } } ] }
---
apiVersion: v1
kind: Service
metadata: { name: teamflow-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow } }
spec: { selector: { app.kubernetes.io/name: teamflow-frontend }, ports: [ { port: 80, targetPort: 80 } ] }
---
# Frontend NÃO faz strip (regra de ouro) e priority 10 < 40 da API -> /teamflow/api vai p/ a API.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: teamflow-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: teamflow } }
spec:
  entryPoints: [web]
  routes:
    - match: PathPrefix(`/teamflow`)
      kind: Rule
      priority: 10
      services: [ { name: teamflow-frontend, port: 80 } ]
