# Val Demo — frontend (Vue SPA) gerado pela Forge. Aditivo ao k8s do backend.
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: valdemo-frontend, namespace: apps, labels: { app.kubernetes.io/name: valdemo-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: valdemo } }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: valdemo-frontend } }
  template:
    metadata: { labels: { app.kubernetes.io/name: valdemo-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: valdemo } }
    spec: { containers: [ { name: frontend, image: valdemo-frontend:local, imagePullPolicy: IfNotPresent, ports: [ { containerPort: 80 } ], readinessProbe: { httpGet: { path: /healthz, port: 80 }, initialDelaySeconds: 3, periodSeconds: 10 } } ] }
---
apiVersion: v1
kind: Service
metadata: { name: valdemo-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: valdemo } }
spec: { selector: { app.kubernetes.io/name: valdemo-frontend }, ports: [ { port: 80, targetPort: 80 } ] }
---
# Frontend NÃO faz strip (regra de ouro) e priority 10 < 40 da API -> /valdemo/api vai p/ a API.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: valdemo-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: valdemo } }
spec:
  entryPoints: [web]
  routes:
    - match: PathPrefix(`/valdemo`)
      kind: Rule
      priority: 10
      services: [ { name: valdemo-frontend, port: 80 } ]
