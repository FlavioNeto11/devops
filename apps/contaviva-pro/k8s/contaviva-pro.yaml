# ContaViva Pro — k8s gerado pela Forge (gymops-style). Imagens :local.
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: contaviva-pro-postgres, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro } }
spec: { accessModes: [ReadWriteOnce], resources: { requests: { storage: 1Gi } } }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: contaviva-pro-postgres, namespace: apps, labels: { app.kubernetes.io/name: contaviva-pro-postgres, app.kubernetes.io/part-of: contaviva-pro } }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector: { matchLabels: { app.kubernetes.io/name: contaviva-pro-postgres } }
  template:
    metadata: { labels: { app.kubernetes.io/name: contaviva-pro-postgres, app.kubernetes.io/part-of: contaviva-pro } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom: [ { secretRef: { name: contaviva-pro-db } } ]
          ports: [ { containerPort: 5432 } ]
          volumeMounts: [ { name: data, mountPath: /var/lib/postgresql/data, subPath: pgdata } ]
      volumes: [ { name: data, persistentVolumeClaim: { claimName: contaviva-pro-postgres } } ]
---
apiVersion: v1
kind: Service
metadata: { name: contaviva-pro-postgres, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro } }
spec: { selector: { app.kubernetes.io/name: contaviva-pro-postgres }, ports: [ { port: 5432, targetPort: 5432 } ] }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: contaviva-pro-redis, namespace: apps, labels: { app.kubernetes.io/name: contaviva-pro-redis, app.kubernetes.io/part-of: contaviva-pro } }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: contaviva-pro-redis } }
  template:
    metadata: { labels: { app.kubernetes.io/name: contaviva-pro-redis, app.kubernetes.io/part-of: contaviva-pro } }
    spec: { containers: [ { name: redis, image: redis:7-alpine, ports: [ { containerPort: 6379 } ] } ] }
---
apiVersion: v1
kind: Service
metadata: { name: contaviva-pro-redis, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro } }
spec: { selector: { app.kubernetes.io/name: contaviva-pro-redis }, ports: [ { port: 6379, targetPort: 6379 } ] }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: contaviva-pro-api
  namespace: apps
  labels: { app.kubernetes.io/name: contaviva-pro-api, app.kubernetes.io/component: api, app.kubernetes.io/part-of: contaviva-pro, devops.flavioneto/app-type: product_software }
  annotations: { devops.flavioneto/app: contaviva-pro, devops.flavioneto/metrics-port: "9464" }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: contaviva-pro-api } }
  template:
    metadata: { labels: { app.kubernetes.io/name: contaviva-pro-api, app.kubernetes.io/component: api, app.kubernetes.io/part-of: contaviva-pro } }
    spec:
      containers:
        - name: api
          image: contaviva-pro-api:local
          imagePullPolicy: IfNotPresent
          command: ["npm", "start"]
          envFrom: [ { secretRef: { name: contaviva-pro-db } }, { secretRef: { name: contaviva-pro-ai, optional: true } }, { secretRef: { name: contaviva-pro-auth, optional: true } } ]
          env:
            - { name: REDIS_URL, value: "redis://contaviva-pro-redis:6379" }
            - { name: METRICS_PORT, value: "9464" }
            - { name: AUTO_MIGRATE, value: "true" }
            - { name: AUTO_SEED, value: "true" }
          ports:
            - { name: http, containerPort: 8080 }
            - { name: metrics, containerPort: 9464 }
          readinessProbe: { httpGet: { path: /health, port: 8080 }, initialDelaySeconds: 8, periodSeconds: 10 }
---
apiVersion: v1
kind: Service
metadata: { name: contaviva-pro-api, namespace: apps, labels: { app.kubernetes.io/name: contaviva-pro-api, app.kubernetes.io/part-of: contaviva-pro } }
spec: { selector: { app.kubernetes.io/name: contaviva-pro-api }, ports: [ { name: http, port: 8080, targetPort: 8080 }, { name: metrics, port: 9464, targetPort: 9464 } ] }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: contaviva-pro-worker
  namespace: apps
  labels: { app.kubernetes.io/name: contaviva-pro-worker, app.kubernetes.io/component: worker, app.kubernetes.io/part-of: contaviva-pro }
  annotations: { devops.flavioneto/app: contaviva-pro, devops.flavioneto/metrics-port: "9464" }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: contaviva-pro-worker } }
  template:
    metadata: { labels: { app.kubernetes.io/name: contaviva-pro-worker, app.kubernetes.io/component: worker, app.kubernetes.io/part-of: contaviva-pro } }
    spec:
      containers:
        - name: worker
          image: contaviva-pro-api:local
          imagePullPolicy: IfNotPresent
          command: ["npm", "run", "worker"]
          envFrom: [ { secretRef: { name: contaviva-pro-db } } ]
          env:
            - { name: REDIS_URL, value: "redis://contaviva-pro-redis:6379" }
            - { name: METRICS_PORT, value: "9464" }
            - { name: AUTO_MIGRATE, value: "false" }
          ports: [ { name: metrics, containerPort: 9464 } ]
---
apiVersion: v1
kind: Service
metadata: { name: contaviva-pro-worker, namespace: apps, labels: { app.kubernetes.io/name: contaviva-pro-worker, app.kubernetes.io/part-of: contaviva-pro } }
spec: { selector: { app.kubernetes.io/name: contaviva-pro-worker }, ports: [ { name: metrics, port: 9464, targetPort: 9464 } ] }
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata: { name: contaviva-pro-api-strip, namespace: apps }
spec: { stripPrefix: { prefixes: ["/contaviva-pro/api"] } }
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: contaviva-pro, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro } }
spec:
  entryPoints: [web]
  routes:
    - match: PathPrefix(`/contaviva-pro/api`)
      kind: Rule
      priority: 40
      services: [ { name: contaviva-pro-api, port: 8080 } ]
      middlewares: [ { name: contaviva-pro-api-strip } ]
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: contaviva-pro, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro, release: kube-prometheus-stack } }
spec:
  selector: { matchLabels: { app.kubernetes.io/part-of: contaviva-pro } }
  namespaceSelector: { matchNames: [apps] }
  endpoints: [ { port: metrics, path: /metrics, interval: 15s } ]
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata: { name: contaviva-pro-slo, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro, release: kube-prometheus-stack } }
spec:
  groups:
    - name: contaviva-pro.slo
      rules:
        - alert: contaviva-proApiDown
          expr: absent(up{job=~".*contaviva-pro-api.*"} == 1)
          for: 3m
          labels: { severity: critical, app: contaviva-pro }
          annotations: { summary: "contaviva-pro: API sem target UP" }
