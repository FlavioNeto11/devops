# ContaViva Pro — frontend (Vue SPA) gerado pela Forge. Aditivo ao k8s do backend.
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: contaviva-pro-frontend, namespace: apps, labels: { app.kubernetes.io/name: contaviva-pro-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: contaviva-pro } }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: contaviva-pro-frontend } }
  template:
    metadata: { labels: { app.kubernetes.io/name: contaviva-pro-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: contaviva-pro } }
    spec: { containers: [ { name: frontend, image: contaviva-pro-frontend:local, imagePullPolicy: IfNotPresent, ports: [ { containerPort: 80 } ], readinessProbe: { httpGet: { path: /healthz, port: 80 }, initialDelaySeconds: 3, periodSeconds: 10 } } ] }
---
apiVersion: v1
kind: Service
metadata: { name: contaviva-pro-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro } }
spec: { selector: { app.kubernetes.io/name: contaviva-pro-frontend }, ports: [ { port: 80, targetPort: 80 } ] }
---
# Frontend NÃO faz strip (regra de ouro) e priority 10 < 40 da API -> /contaviva-pro/api vai p/ a API.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: contaviva-pro-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: contaviva-pro } }
spec:
  entryPoints: [web]
  routes:
    - match: PathPrefix(`/contaviva-pro`)
      kind: Rule
      priority: 10
      services: [ { name: contaviva-pro-frontend, port: 80 } ]
