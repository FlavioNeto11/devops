{
  "name": "portal-recorder-api",
  "version": "0.1.0",
  "description": "Portal capture recorder: API for portals, capture sessions, annotations and normalized portal contracts. Browser capture lives in the recorder service.",
  "type": "module",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.16.3"
  }
}
