// LOCKED — gerado de REQ-HELPFLOW-0008 / quality_scenarios[0] (NFR) por make-test-suite.mjs. NÃO EDITAR (mudar exige spec + regenerar).
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { get, post, del, sleep, LIVE } from '../_lib.mjs';

test('REQ-HELPFLOW-0008 NFR1 [O frontend Vue SPA DEVE herdar identidade visual, tema (claro/escuro), acessibilidade e navegação da plataforma via desi]: packages/design-tokens importado no projeto; build sincroniza tokens gerados em src/design-tokens.generated.css', { skip: 'NFR não-automatizável aqui — verificar em forge-tests/monitoring' }, () => {});
