# HelpFlow - Service Desk — frontend (Vue SPA) gerado pela Forge. Aditivo ao k8s do backend.
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: helpflow-frontend, namespace: apps, labels: { app.kubernetes.io/name: helpflow-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: helpflow } }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: helpflow-frontend } }
  template:
    metadata: { labels: { app.kubernetes.io/name: helpflow-frontend, app.kubernetes.io/component: frontend, app.kubernetes.io/part-of: helpflow } }
    spec: { containers: [ { name: frontend, image: helpflow-frontend:local, imagePullPolicy: IfNotPresent, ports: [ { containerPort: 80 } ], readinessProbe: { httpGet: { path: /healthz, port: 80 }, initialDelaySeconds: 3, periodSeconds: 10 } } ] }
---
apiVersion: v1
kind: Service
metadata: { name: helpflow-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: helpflow } }
spec: { selector: { app.kubernetes.io/name: helpflow-frontend }, ports: [ { port: 80, targetPort: 80 } ] }
---
# Frontend NÃO faz strip (regra de ouro) e priority 10 < 40 da API -> /helpflow/api vai p/ a API.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: helpflow-frontend, namespace: apps, labels: { app.kubernetes.io/part-of: helpflow } }
spec:
  entryPoints: [web]
  routes:
    - match: PathPrefix(`/helpflow`)
      kind: Rule
      priority: 10
      services: [ { name: helpflow-frontend, port: 80 } ]
