# Forja Demo — k8s gerado pela Forge. Imagens :local (IfNotPresent).
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: forjademo-postgres, namespace: apps, labels: { app.kubernetes.io/part-of: forjademo } }
spec: { accessModes: [ReadWriteOnce], resources: { requests: { storage: 1Gi } } }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: forjademo-postgres, namespace: apps, labels: { app.kubernetes.io/name: forjademo-postgres, app.kubernetes.io/part-of: forjademo } }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector: { matchLabels: { app.kubernetes.io/name: forjademo-postgres } }
  template:
    metadata: { labels: { app.kubernetes.io/name: forjademo-postgres, app.kubernetes.io/part-of: forjademo } }
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom: [ { secretRef: { name: forjademo-db } } ]
          ports: [ { containerPort: 5432 } ]
          volumeMounts: [ { name: data, mountPath: /var/lib/postgresql/data, subPath: pgdata } ]
      volumes: [ { name: data, persistentVolumeClaim: { claimName: forjademo-postgres } } ]
---
apiVersion: v1
kind: Service
metadata: { name: forjademo-postgres, namespace: apps, labels: { app.kubernetes.io/part-of: forjademo } }
spec: { selector: { app.kubernetes.io/name: forjademo-postgres }, ports: [ { port: 5432, targetPort: 5432 } ] }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forjademo-api
  namespace: apps
  labels: { app.kubernetes.io/name: forjademo-api, app.kubernetes.io/component: api, app.kubernetes.io/part-of: forjademo, devops.flavioneto/app-type: product_software }
  annotations: { devops.flavioneto/app: forjademo, devops.flavioneto/metrics-port: "9464" }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: forjademo-api } }
  template:
    metadata: { labels: { app.kubernetes.io/name: forjademo-api, app.kubernetes.io/component: api, app.kubernetes.io/part-of: forjademo } }
    spec:
      containers:
        - name: api
          image: forjademo-api:local
          imagePullPolicy: IfNotPresent
          command: ["npm", "start"]
          envFrom: [ { secretRef: { name: forjademo-db } } ]
          env:
            - { name: METRICS_PORT, value: "9464" }
            - { name: AUTO_MIGRATE, value: "true" }
            - { name: AUTO_SEED, value: "true" }
          ports:
            - { name: http, containerPort: 8080 }
            - { name: metrics, containerPort: 9464 }
          readinessProbe: { httpGet: { path: /health, port: 8080 }, initialDelaySeconds: 8, periodSeconds: 10 }
---
apiVersion: v1
kind: Service
metadata: { name: forjademo-api, namespace: apps, labels: { app.kubernetes.io/name: forjademo-api, app.kubernetes.io/part-of: forjademo } }
spec: { selector: { app.kubernetes.io/name: forjademo-api }, ports: [ { name: http, port: 8080, targetPort: 8080 }, { name: metrics, port: 9464, targetPort: 9464 } ] }
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata: { name: forjademo-api-strip, namespace: apps }
spec: { stripPrefix: { prefixes: ["/forjademo/api"] } }
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata: { name: forjademo, namespace: apps, labels: { app.kubernetes.io/part-of: forjademo } }
spec:
  entryPoints: [web]
  routes:
    - match: PathPrefix(`/forjademo/api`)
      kind: Rule
      priority: 40
      services: [ { name: forjademo-api, port: 8080 } ]
      middlewares: [ { name: forjademo-api-strip } ]
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: forjademo, namespace: apps, labels: { app.kubernetes.io/part-of: forjademo, release: kube-prometheus-stack } }
spec:
  selector: { matchLabels: { app.kubernetes.io/part-of: forjademo } }
  namespaceSelector: { matchNames: [apps] }
  endpoints: [ { port: metrics, path: /metrics, interval: 15s } ]
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata: { name: forjademo-slo, namespace: apps, labels: { app.kubernetes.io/part-of: forjademo, release: kube-prometheus-stack } }
spec:
  groups:
    - name: forjademo.slo
      rules:
        - alert: forjademoApiDown
          expr: absent(up{job=~".*forjademo-api.*"} == 1)
          for: 3m
          labels: { severity: critical, app: forjademo }
          annotations: { summary: "forjademo: API sem target UP" }
