// LOCKED — gerado de REQ-NEUROEVOLUI-0008 / quality_scenarios[0] (NFR) por make-test-suite.mjs. NÃO EDITAR (mudar exige spec + regenerar).
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { get, post, del, sleep, LIVE } from '../_lib.mjs';

test('REQ-NEUROEVOLUI-0008 NFR1 [O sistema DEVE documentar sua API de forma canônica em OpenAPI, gerar tipos e operações a partir dele, e validar contrat]: Arquivo OpenAPI canônico (openapi.yaml) em src/openapi/', { skip: 'NFR não-automatizável aqui — verificar em forge-tests/monitoring' }, () => {});
