// LOCKED — gerado de REQ-NEUROEVOLUI-0009 / quality_scenarios[0] (NFR) por make-test-suite.mjs. NÃO EDITAR (mudar exige spec + regenerar).
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { get, post, del, sleep, LIVE } from '../_lib.mjs';

test('REQ-NEUROEVOLUI-0009 NFR1 [O sistema DEVE utilizar design system centralizado (tokens + componentes da plataforma) para oferecer interface consiste]: Projeto frontend importa packages/design-tokens (nunca edita .generated.css)', { skip: 'NFR não-automatizável aqui — verificar em forge-tests/monitoring' }, () => {});
