apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
resources:
  - besc.yaml
  - besc-frontend.yaml
  - postgres.yaml
  - besu.yaml
  # SealedSecrets gerados pelo operador (kubeseal) — exemplo em secret.example.yaml.tmpl
  - sealed-besc-db.yaml
  - sealed-besc-config.yaml
