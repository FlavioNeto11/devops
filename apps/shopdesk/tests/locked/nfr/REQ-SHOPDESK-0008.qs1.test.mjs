// LOCKED — gerado de REQ-SHOPDESK-0008 / quality_scenarios[0] (NFR) por make-test-suite.mjs. NÃO EDITAR (mudar exige spec + regenerar).
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { get, post, del, sleep, LIVE } from '../_lib.mjs';

test('REQ-SHOPDESK-0008 NFR1 [uma rota é adicionada/alterada sem atualizar o OpenAPI]: 100% das rotas implementadas estão documentadas e vice-versa (zero drift)', { skip: 'NFR não-automatizável aqui — verificar em forge-tests/monitoring' }, () => {});
