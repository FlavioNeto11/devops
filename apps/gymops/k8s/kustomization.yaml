apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Aplica os manifests do GymOps. NAO inclui secret.example.yaml: o Secret real (gymops-config)
# e criado no cluster fora do git (a partir do .env.docker) e nao e gerenciado/prunado pelo Argo.
resources:
  - gymops.yaml
  - servicemonitor.yaml
