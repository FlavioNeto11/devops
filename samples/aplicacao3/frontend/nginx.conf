server {
  listen 80;
  location = /healthz { return 200 'ok'; add_header Content-Type text/plain; }
  location = /aplicacao3 { return 301 /aplicacao3/; }
  location /aplicacao3/ {
    alias /usr/share/nginx/html/;
    index index.html;
    try_files $uri $uri/ /aplicacao3/index.html;
  }
}
