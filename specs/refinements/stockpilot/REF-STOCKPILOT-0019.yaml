id: REF-STOCKPILOT-0019
slug: ref-stockpilot-0019
title: "Detalhe da auditoria"
kind: screen
status: approved

scope:
  product_scope: stockpilot
  applies_to: screen

anchors:
  - requirement_id: REQ-STOCKPILOT-0004
    relation: implements

surface:
  route: "/audit/:id"
  name: "Detalhe da auditoria"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "event"
      source: "api:/audit/:id"
      editable: false
      format: "evento de auditoria completo (ator, acao, payload redigido)"
    - field: "context"
      source: "api:/audit/:id"
      editable: false
      format: "contexto do evento e recurso afetado"
  interactions:
    - trigger: "abrir /audit/:id"
      action: "carrega o detalhe do evento de auditoria"
      result: "renderiza ator, acao, contexto e payload redigido"

source:
  source_paths:
    - specs/products/stockpilot/product.json
    - apps/stockpilot/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
