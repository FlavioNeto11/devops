id: REF-STOCKPILOT-0018
slug: ref-stockpilot-0018
title: "Auditoria do fornecedor"
kind: screen
status: approved

scope:
  product_scope: stockpilot
  applies_to: screen

anchors:
  - requirement_id: REQ-STOCKPILOT-0004
    relation: implements

surface:
  route: "/audit"
  name: "Auditoria do fornecedor"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "audit_events"
      source: "api:/audit"
      editable: false
      format: "trilha de auditoria com redacao de dados sensiveis"
    - field: "supplier_filter"
      source: "ui:local"
      editable: true
      format: "filtro por fornecedor"
  interactions:
    - trigger: "abrir /audit"
      action: "carrega a trilha de auditoria"
      result: "renderiza eventos com ator, acao e data"
    - trigger: "clicar em um evento"
      action: "navega para /audit/:id"
      result: "abre o detalhe da auditoria"

source:
  source_paths:
    - specs/products/stockpilot/product.json
    - apps/stockpilot/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
