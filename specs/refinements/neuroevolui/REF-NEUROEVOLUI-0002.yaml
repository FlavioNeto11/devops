id: "REF-NEUROEVOLUI-0002"
slug: "ref-neuroevolui-0002"
title: "Pacientes"
kind: "screen"
status: "approved"
scope:
  product_scope: "neuroevolui"
anchors:
  - requirement_id: "REQ-NEUROEVOLUI-0004"
    relation: "implements"
surface:
  route: "/patients"
  name: "Pacientes"
  roles:
    - "user"
behavior:
  states:
    - name: "normal"
    - name: "loading"
    - name: "empty"
    - name: "error"
  data:
    - field: "patients.items"
      source: "api:/api/patients"
      editable: false
    - field: "pagination"
      source: "api:/api/patients?page=&size="
      editable: false
    - field: "query.search"
      source: "estado local (filtro de busca)"
      editable: true
  interactions:
    - trigger: "abrir a tela"
      action: "carrega a lista de pacientes paginada"
      result: "renderiza a tabela com os itens"
    - trigger: "digitar na busca"
      action: "filtra a lista pelo termo"
      result: "atualiza os resultados exibidos"
    - trigger: "clicar em Novo"
      action: "navega para /patients/new"
      result: "abre o formulário de criação de paciente"
    - trigger: "clicar em uma linha"
      action: "navega para o detalhe do paciente"
      result: "abre a tela de detalhe"
source:
  source_paths:
    - "specs/products/neuroevolui/product.json"
    - "apps/neuroevolui/frontend/src/views"
version:
  baseline_version: "1.0.0"
  item_revision: 1
