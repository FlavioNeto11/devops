id: "REF-NEUROEVOLUI-0022"
slug: "ref-neuroevolui-0022"
title: "Detalhe do registro"
kind: "screen"
status: "approved"
scope:
  product_scope: "neuroevolui"
anchors:
  - requirement_id: "REQ-NEUROEVOLUI-0003"
    relation: "implements"
surface:
  route: "/records/:id"
  name: "Detalhe do registro"
  roles:
    - "user"
behavior:
  states:
    - name: "normal"
    - name: "loading"
    - name: "empty"
    - name: "error"
  data:
    - field: "registro.id"
      source: "route param :id"
      editable: false
    - field: "registro.fields"
      source: "api:/api/records/:id"
      editable: false
  interactions:
    - trigger: "abrir a tela"
      action: "carrega o registro por :id"
      result: "renderiza os campos do registro"
    - trigger: "clicar em Editar"
      action: "navega para /records/:id/edit"
      result: "abre o formulário de edição"
source:
  source_paths:
    - "specs/products/neuroevolui/product.json"
    - "apps/neuroevolui/frontend/src/views"
version:
  baseline_version: "1.0.0"
  item_revision: 1
