id: REF-SHOPDESK-0030
slug: ref-shopdesk-0030
title: "Saude do sistema"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0001
    relation: implements
  - requirement_id: REQ-SHOPDESK-0004
    relation: relates_to

surface:
  route: "/settings/health"
  name: "Saude do sistema"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "view"
      source: "api:/settings/health"
      editable: false
      format: "estado da tela"
  interactions:
    - trigger: "abrir /settings/health"
      action: "carrega o estado da tela especializada"
      result: "renderiza a interface dedicada"
    - trigger: "acao primaria da tela"
      action: "executa o fluxo especifico desta superficie"
      result: "reflete o resultado na propria tela"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
