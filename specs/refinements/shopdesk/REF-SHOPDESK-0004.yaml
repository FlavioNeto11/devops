id: REF-SHOPDESK-0004
slug: ref-shopdesk-0004
title: "Detalhe do produto"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0005
    relation: implements

surface:
  route: "/products/:id"
  name: "Detalhe do produto"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "record"
      source: "api:/products/:id"
      editable: false
      format: "objeto do dominio"
  interactions:
    - trigger: "abrir /products/:id"
      action: "carrega o registro de products por id"
      result: "renderiza os dados do registro"
    - trigger: "clicar em Editar"
      action: "navega para a tela de edicao"
      result: "abre /products/:id/edit"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
