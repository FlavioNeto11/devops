id: REF-SHOPDESK-0020
slug: ref-shopdesk-0020
title: "Detalhe da NF-e"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0004
    relation: implements

surface:
  route: "/invoices/:id"
  name: "Detalhe da NF-e"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "record"
      source: "api:/invoices/:id"
      editable: false
      format: "objeto do dominio"
  interactions:
    - trigger: "abrir /invoices/:id"
      action: "carrega o registro de invoices por id"
      result: "renderiza os dados do registro"
    - trigger: "clicar em Editar"
      action: "navega para a tela de edicao"
      result: "abre /invoices/:id/edit"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
