id: REF-SHOPDESK-0005
slug: ref-shopdesk-0005
title: "Editar produto"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0005
    relation: implements
  - requirement_id: REQ-SHOPDESK-0006
    relation: relates_to

surface:
  route: "/products/:id/edit"
  name: "Editar produto"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "record"
      source: "api:/products/:id"
      editable: false
      format: "objeto do dominio"
    - field: "form"
      source: "estado-local (rascunho a partir do record)"
      editable: true
  interactions:
    - trigger: "abrir /products/:id/edit"
      action: "carrega o registro de products para edicao"
      result: "preenche o formulario"
    - trigger: "clicar em Salvar"
      action: "envia PUT/PATCH para api:/products/:id"
      result: "persiste e retorna ao detalhe"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
