id: REF-SHOPDESK-0010
slug: ref-shopdesk-0010
title: "Checkout"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0003
    relation: implements

surface:
  route: "/checkout/:cartId"
  name: "Checkout"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "view"
      source: "api:/checkout/:param"
      editable: false
      format: "estado da tela"
  interactions:
    - trigger: "abrir /checkout/:cartId"
      action: "carrega o estado da tela especializada"
      result: "renderiza a interface dedicada"
    - trigger: "acao primaria da tela"
      action: "executa o fluxo especifico desta superficie"
      result: "reflete o resultado na propria tela"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
