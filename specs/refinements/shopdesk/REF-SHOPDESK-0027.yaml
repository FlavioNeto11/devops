id: REF-SHOPDESK-0027
slug: ref-shopdesk-0027
title: "Configuracoes da loja"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0002
    relation: implements
  - requirement_id: REQ-SHOPDESK-0004
    relation: relates_to
  - requirement_id: REQ-SHOPDESK-0007
    relation: relates_to

surface:
  route: "/settings/store"
  name: "Configuracoes da loja"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "view"
      source: "api:/settings/store"
      editable: false
      format: "estado da tela"
  interactions:
    - trigger: "abrir /settings/store"
      action: "carrega o estado da tela especializada"
      result: "renderiza a interface dedicada"
    - trigger: "acao primaria da tela"
      action: "executa o fluxo especifico desta superficie"
      result: "reflete o resultado na propria tela"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
