id: REF-SHOPDESK-0008
slug: ref-shopdesk-0008
title: "Detalhe do pedido"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0003
    relation: implements
  - requirement_id: REQ-SHOPDESK-0004
    relation: relates_to
  - requirement_id: REQ-SHOPDESK-0007
    relation: relates_to
  - requirement_id: REQ-SHOPDESK-0006
    relation: relates_to

surface:
  route: "/orders/:id"
  name: "Detalhe do pedido"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "record"
      source: "api:/orders/:id"
      editable: false
      format: "objeto do dominio"
  interactions:
    - trigger: "abrir /orders/:id"
      action: "carrega o registro de orders por id"
      result: "renderiza os dados do registro"
    - trigger: "clicar em Editar"
      action: "navega para a tela de edicao"
      result: "abre /orders/:id/edit"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
