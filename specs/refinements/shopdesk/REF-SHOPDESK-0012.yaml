id: REF-SHOPDESK-0012
slug: ref-shopdesk-0012
title: "Detalhe do carrinho"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0003
    relation: implements

surface:
  route: "/carts/:id"
  name: "Detalhe do carrinho"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "record"
      source: "api:/carts/:id"
      editable: false
      format: "objeto do dominio"
  interactions:
    - trigger: "abrir /carts/:id"
      action: "carrega o registro de carts por id"
      result: "renderiza os dados do registro"
    - trigger: "clicar em Editar"
      action: "navega para a tela de edicao"
      result: "abre /carts/:id/edit"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
