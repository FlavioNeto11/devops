id: REF-SHOPDESK-0026
slug: ref-shopdesk-0026
title: "Editar usuario"
kind: screen
status: approved

scope:
  product_scope: shopdesk
  applies_to: screen

anchors:
  - requirement_id: REQ-SHOPDESK-0002
    relation: implements

surface:
  route: "/settings/users/:id/edit"
  name: "Editar usuario"
  roles:
    - user

behavior:
  states:
    - name: normal
    - name: loading
    - name: empty
    - name: error
  data:
    - field: "record"
      source: "api:/users/:id"
      editable: false
      format: "objeto do dominio"
    - field: "form"
      source: "estado-local (rascunho a partir do record)"
      editable: true
  interactions:
    - trigger: "abrir /settings/users/:id/edit"
      action: "carrega o registro de users para edicao"
      result: "preenche o formulario"
    - trigger: "clicar em Salvar"
      action: "envia PUT/PATCH para api:/users/:id"
      result: "persiste e retorna ao detalhe"

source:
  source_paths:
    - specs/products/shopdesk/product.json
    - apps/shopdesk/frontend/src/views

version:
  baseline_version: 1.0.0
  item_revision: 1
