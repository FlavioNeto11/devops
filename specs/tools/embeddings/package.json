{
  "name": "@flavioneto11/specs-embeddings",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gera specs/baseline/embeddings.json (embeddings locais all-MiniLM, sem API paga) para similaridade semantica no Reqhub. Pacote ISOLADO: o gate specs-governance NAO instala isto (a baseline e o embeddings.json sao commitados).",
  "scripts": { "emit": "node emit-embeddings.mjs" },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
