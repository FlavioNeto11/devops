{
  "name": "@flavioneto11/specs-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ferramentas da base de requisitos (fonte da verdade): valida artefatos contra o schema e gera a baseline consumida pela UI e pelo Claude.",
  "scripts": {
    "seed": "node seed.mjs",
    "build": "node build-baseline.mjs",
    "validate": "node build-baseline.mjs --check",
    "test": "node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.5.1"
  }
}
