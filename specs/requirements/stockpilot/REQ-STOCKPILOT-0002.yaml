id: REQ-STOCKPILOT-0002
title: "Autenticação federada e isolamento multi-tenant"
type: functional
status: approved
owner: flavio
scope:
  applies_to: capability
  product_scope: stockpilot
  blueprint: node-api-vue-spa
statement: "O sistema DEVE suportar autenticação via OIDC (Keycloak, realm nvit) com sessão própria do app, e isolar completamente dados por tenant (empresa) com RBAC em cascata (owner > org_manager > unit_manager > area_leader > assignee)."
business_rationale: "Capacidade proposta pela IA da Forja a partir do brief do produto."
source:
  origin: "FORGE — concepção pela IA (propose_requirements)"
  source_paths: ["specs/products/stockpilot/product.json"]
priority: critical
criticality: medium
architectural_significance: true
capability_blocks: ["oidc-sessao", "rbac-multitenant"]
acceptance_criteria:
  - "Login inicia fluxo PKCE contra Keycloak realm nvit"
  - "Backend valida token via /userinfo do Keycloak e emite JWT curto próprio"
  - "Frontend guarda sessão (JWT curto) em cookie HttpOnly"
  - "Toda query de repositório inclui filtro WHERE tenant_id = ? obrigatório"
  - "Toda tabela de negócio possui coluna tenant_id + índice (tenant_id, id)"
  - "Usuário não pode acessar dados de outro tenant mesmo manipulando JWT"
verification_method: [test-integration]
version:
  baseline_version: "1.0.0"
  item_revision: 1
