id: REQ-GYMOPS-NFR-002
slug: req-gymops-nfr-002
title: "Segurança: RBAC sempre no backend, sem exposição de IDs sequenciais"
type: non-functional
status: approved
owner: plataforma-digital
scope:
  applies_to: product
  product_scope: gymops
statement: 'O sistema deve atender ao atributo de qualidade "Segurança: RBAC sempre no backend, sem exposição de IDs sequenciais": Retorna 404 (sobre 403 para evitar enumeração) se permissão negada. Nunca expõe IDs sequenciais. Validação Zod em entrada.'
source:
  stakeholder: gymops
  origin: codebase apps/gymops + research-2026-06
priority: high
criticality: high
architectural_significance: true
quality_scenarios:
  - source: Agente malicioso tentando escalação de privilégio via curl
    stimulus: Solicita PATCH /activities/:id com dados de atividade de outro usuário ou organização
    environment: Qualquer rede; qualquer token válido de usuário sem acesso
    artifact: Backend Fastify com resolveActivityPermission() obrigatório + UUIDs v4 em todas as tabelas
    response: Retorna 404 (sobre 403 para evitar enumeração) se permissão negada. Nunca expõe IDs sequenciais. Validação Zod em entrada.
    response_measure: Status HTTP correto + auditoria log do acesso denegado
links:
  - type: constrains
    target: REQ-GYMOPS-0003
  - type: constrains
    target: REQ-GYMOPS-0004
version:
  baseline_version: 1.0.0
  item_revision: 1
  semantic_change: none
  change_reason: baseline inicial (bootstrap)
