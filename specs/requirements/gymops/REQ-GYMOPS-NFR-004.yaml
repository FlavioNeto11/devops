id: REQ-GYMOPS-NFR-004
slug: req-gymops-nfr-004
title: Disponibilidade de IA com fallback gracioso (nunca 500 por falha OpenAI)
type: non-functional
status: approved
owner: plataforma-digital
scope:
  applies_to: product
  product_scope: gymops
statement: 'O sistema deve atender ao atributo de qualidade "Disponibilidade de IA com fallback gracioso (nunca 500 por falha OpenAI)": Retorna fallback (valores default sensatos) sem erro 500. Usuário vê sugestão degradada mas continua operando.'
source:
  stakeholder: gymops
  origin: codebase apps/gymops + research-2026-06
priority: high
criticality: high
architectural_significance: false
quality_scenarios:
  - source: Usuário clica 'Sugerir checklist' enquanto OpenAI API está offline
    stimulus: POST /ai/activities/checklist com OpenAI timeout ou 5xx
    environment: Qualquer chamada síncrona (<10s) ou assíncrona em worker (<60s)
    artifact: callAI(fn, fallback, timeoutMs) em todos os endpoints /ai/*. Rate limit 10/min por userId.
    response: Retorna fallback (valores default sensatos) sem erro 500. Usuário vê sugestão degradada mas continua operando.
    response_measure: "Status 200 com confidence: 0 ou lista vazia (nunca 5xx)"
version:
  baseline_version: 1.0.0
  item_revision: 1
  semantic_change: none
  change_reason: baseline inicial (bootstrap)
