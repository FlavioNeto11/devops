id: REQ-SHOPDESK-0002
title: "Isolamento multi-tenant + RBAC + Login SSO"
type: functional
status: approved
owner: flavio
scope:
  applies_to: capability
  product_scope: shopdesk
  blueprint: node-api-vue-spa
statement: "O sistema DEVE isolar dados por tenant (loja), controlar acesso por papéis em cascata (owner > admin > operador) e autenticar operadores via OIDC (Keycloak)."
business_rationale: "Capacidade proposta pela IA da Forja a partir do brief do produto."
source:
  origin: "FORGE — concepção pela IA (propose_requirements)"
  source_paths: ["specs/products/shopdesk/product.json"]
priority: critical
criticality: medium
architectural_significance: true
capability_blocks: ["rbac-multitenant", "oidc-sessao"]
acceptance_criteria:
  - "Toda tabela de dados de negócio carrega tenant_id e toda query é escopada por tenant do usuário"
  - "Papéis em cascata: owner > admin > operador; deny por padrão fora do escopo"
  - "Login via Keycloak (realm nvit) com fluxo PKCE; backend valida token em /userinfo e emite JWT curto da sessão do app"
  - "Soft-delete em entities críticas; auditoria de acesso por usuário/tenant/recurso"
  - "Teste de isolamento: usuário da loja A não vê dados da loja B"
verification_method: [test-integration]
version:
  baseline_version: "1.0.0"
  item_revision: 1
