id: REQ-CONSOLE-NFR-006
slug: req-console-nfr-006
title: SSE keep-alive and reconnection resilience
type: non-functional
status: approved
owner: plataforma-digital
scope:
  applies_to: product
  product_scope: console
statement: 'O sistema deve atender ao atributo de qualidade "SSE keep-alive and reconnection resilience": connection stays alive across idle periods; auto-reconnect on disconnect; no data loss after reconnect'
source:
  stakeholder: console
  origin: codebase console + mapeamento multi-agente 2026-06-14
  source_paths:
    - console
priority: high
criticality: high
architectural_significance: false
quality_scenarios:
  - source: frontend EventSource connection through proxy
    stimulus: proxy idle timeout (e.g., Traefik buffering) or brief network loss
    environment: HTTP/1.1 persistent connection, typical proxy config
    artifact: backend keep-alive comment frames (15s interval); EventSource auto-reconnect
    response: connection stays alive across idle periods; auto-reconnect on disconnect; no data loss after reconnect
    response_measure: keep-alive frame sent every 15s; EventSource reconnects within 1-3s; snapshot sent on reconnect
version:
  baseline_version: 1.0.0
  item_revision: 1
  semantic_change: none
  change_reason: baseline inicial (bootstrap)
