id: REQ-CONSOLE-0008
slug: req-console-0008
title: Display Traefik IngressRoutes with hosts and paths
type: functional
status: approved
owner: plataforma-digital
scope:
  product_scope: console
  applies_to: product
statement: "O backend lista IngressRoutes do Traefik via GET /ingressroutes (CRD traefik.io/v1alpha1). Para cada rota extrai: hosts (via regex Host(...)), paths (Path/PathPrefix), services, middlewares e priority. Frontend exibe roteamento e como cada app esta exposta no host unico (xpto.localhost ou dev.nvit.com.br futuro)."
source:
  stakeholder: console
  origin: codebase console + mapeamento multi-agente 2026-06-14
priority: high
criticality: high
architectural_significance: false
version:
  baseline_version: 1.0.0
  item_revision: 1
  semantic_change: none
  change_reason: baseline inicial (bootstrap)
