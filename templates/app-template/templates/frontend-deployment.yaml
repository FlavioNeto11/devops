{{- /*
Gera um Deployment para cada service com type == "frontend".
Frontends sao SPAs servidas por nginx sob o subpath (base path). A imagem ja
e buildada com o base path correto (ex.: VITE_BASE_PATH=/aplicacao1/), portanto
NAO ha strip de prefixo no Ingress.
*/ -}}
{{- range $name, $svc := .Values.services }}
{{- if eq $svc.type "frontend" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app-template.fullname" (dict "root" $ "name" $name) }}
  namespace: {{ $.Values.app.namespace }}
  labels:
    {{- include "app-template.commonLabels" (dict "root" $ "name" $name "component" $svc.type) | nindent 4 }}
  annotations:
    {{- include "app-template.publishAnnotations" $ | nindent 4 }}
spec:
  replicas: {{ default 1 $svc.replicas }}
  selector:
    matchLabels:
      {{- include "app-template.selectorLabels" (dict "root" $ "name" $name) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app-template.commonLabels" (dict "root" $ "name" $name "component" $svc.type) | nindent 8 }}
      annotations:
        {{- include "app-template.publishAnnotations" $ | nindent 8 }}
    spec:
      containers:
        - name: {{ $name }}
          image: {{ include "app-template.image" (dict "root" $ "name" $name "svc" $svc) }}
          imagePullPolicy: {{ $.Values.defaults.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ $svc.port }}
              protocol: TCP
          {{- with $svc.env }}
          env:
            {{- range $k, $v := . }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ $svc.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            {{- include "app-template.resources" (dict "root" $ "svc" $svc) | nindent 12 }}
{{- end }}
{{- end }}
